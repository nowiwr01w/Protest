package com.nowiwr01p.domain.auth.validators

/**
 * User password validator declaration.
 * Check password string by internal validation password rules.
 * */
interface PasswordValidator : Validator<String>